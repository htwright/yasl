# YASL Project Config

<!--
  Generated by /setup (YASL). Skills read this file at startup.
  Edit the values below to match your project — you own this file.
  The placeholder markers {{LIKE_THIS}} are filled in by /setup;
  if you wrote this by hand, just replace them with real values.
-->

**Project:** {{PROJECT_NAME}}

## Commands

YASL skills run these on your behalf. `/validate`, `/plan`, `/build`, and `/review` read them.

- **Type check:** `{{TYPE_CHECK_CMD}}`  <!-- e.g., pnpm type-check, tsc --noEmit, mix compile --warnings-as-errors -->
- **Lint:** `{{LINT_CMD}}`  <!-- e.g., pnpm lint, eslint ., ruff check -->
- **Test:** `{{TEST_CMD}}`  <!-- e.g., pnpm test, pytest, go test ./... -->

### Optional commands

Leave any line as `(not configured)` if it doesn't apply.

- **Secondary test suite:** `{{TEST_SECONDARY_CMD}}`  <!-- e.g., a separate frontend test runner -->
- **Production smoke test:** `{{TEST_PRODUCTION_CMD}}`  <!-- hits deployed services; used by /validate production -->
- **Dev server:** `{{DEV_CMD}}`  <!-- e.g., pnpm dev, uvicorn app:main --reload -->

## Commit Scopes

`/commit` suggests one of these as the conventional-commit scope.

- `{{SCOPE_1}}`
- `{{SCOPE_2}}`
- `{{SCOPE_3}}`

<!-- Add as many as make sense for your repo. For monorepos, usually one per package. -->

## Example Patterns

`/plan` uses these when it wants to point `/build` at a file to model a new implementation on. Pick the cleanest representative of each pattern in your codebase.

- **Route / endpoint pattern:** `{{ROUTE_EXAMPLE}}`
- **Schema / model pattern:** `{{SCHEMA_EXAMPLE}}`
- **Test pattern:** `{{TEST_EXAMPLE}}`

## Paths

- **Docs root:** `{{DOCS_ROOT}}`  <!-- where problem maps, specs, plans live; default: docs/ -->

---

> This file lives at `.claude/yasl.config.md` in your repo. If you move it, update the Startup sections in each skill file so they know where to look.
